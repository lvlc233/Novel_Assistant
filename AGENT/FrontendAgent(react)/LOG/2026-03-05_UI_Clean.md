# UI 清理记录

## 时间
2026年03月05日 04:15

## 目标
根据用户要求，移除 `PluginSettingsModal.tsx` 和 `ConfigRenderer.tsx` 中的半透明背景和 backdrop filters，使 UI 看起来更加“坚实”和整洁。

## 变更范围

### `src/components/plugins/PluginSettingsModal.tsx`
1.  **Overlay**: `bg-black/20 backdrop-blur-sm` -> `bg-black/40` (Solid overlay, no blur)
2.  **DataSkeleton**: `bg-white/50` -> `bg-white`
3.  **Card Header**: `bg-white/50 backdrop-blur-md` -> `bg-white`
4.  **Card Content**: `bg-gray-50/30` -> `bg-gray-50`
5.  **Icon Border**: `border-black/5` -> `border-gray-200`

### `src/components/dashboard/plugin-renderers/ConfigRenderer.tsx`
1.  **Sticky Footer**: `bg-white/95 backdrop-blur-sm` -> `bg-white` (Already had border-t)

## 验证方式
代码审查，确保所有 translucent 类已被替换为 solid 类。

## 结果
完成所有替换，UI 风格统一为 Solid/Opaque 风格。
