# 编辑器样式重构日志

## 基本信息
- **时间**: 2026-01-21 12:45
- **目标**: 根据参考设计图，彻底重构编辑器页面样式，实现 AI 助手与编辑器的双栏布局。
- **变更范围**:
    - `src/app/editor/page.tsx`: 布局调整为 `Flex` 左右分栏，左侧为固定宽度的 AI 助手，右侧为编辑器。
    - `src/components/editor/AIAssistant.tsx`: 新增 AI 助手组件，包含聊天气泡、快捷指令、输入框和侧边悬浮挂件。
    - `src/components/editor/DocumentEditor.tsx`: 改造编辑器布局，将目录 (`TableOfContents`) 整合为编辑器内部的左侧边栏，优化顶部工具栏和底部保存按钮样式。
    - `src/components/table-of-contents/TableOfContents.tsx`: 样式重构，改为透明背景、极简风格的树状列表，底部增加菱形装饰。

## 验证方式与结果
1.  **验证方式**:
    - 访问 `/editor?novelId=1&initialChapterId=c1`。
    - 检查页面是否分为左右两部分：
        - 左侧：宽度约 400px 的 AI 助手，包含对话列表和底部输入框。
        - 右侧：编辑器区域，内部包含左侧窄目录栏（半透明背景）、顶部标题栏、中间编辑区。
    - 检查 AI 助手输入框是否可用，发送消息是否有模拟回复。
    - 检查右侧目录是否可以点击切换章节。
    - 检查顶部标题栏是否可以修改标题，右下角保存按钮是否正常。

2.  **结果**:
    - 页面布局已完全重构，符合参考图的结构。
    - AI 助手组件功能正常，交互流畅。
    - 编辑器与目录的集成度更高，视觉风格更加统一、现代。
