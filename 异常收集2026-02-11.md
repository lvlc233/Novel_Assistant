# 前端
## app/works: page.tsx
### 异常
在 line-52~line-57:
    类型“KnowledgeBaseMeta”上不存在属性“name”
    类型“KnowledgeBaseMeta”上不存在属性“tags”
### 调查分析
在types/knowledge-base.ts中:
export interface KnowledgeBaseMeta {
  id: string;
  title: string;
  description?: string;
  enabled?: boolean;
  create_at?: string;
}
确实没有name,而应该是title
在统一文档中:
line-328~line-365:存在部分使用的是name,这是错误的,应该是title
## app/works/[id]