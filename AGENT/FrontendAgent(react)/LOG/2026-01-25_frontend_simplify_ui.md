# 前端变更日志 - 2026-01-25 (UI简化)

## 变更信息
- **时间**: 2026-01-25 12:35
- **提交人**: FrontendAgent(react)
- **目标**: 根据用户指令，精简作品创建配置页的UI，移除不必要的选择项。

## 变更内容

### 1. UI精简 (CreateNovelCard.tsx)
- **隐藏题材选择**: 移除了"基本信息"中的"题材"(Genre)选择区域。
  - *注*: 数据层仍保留 `genre` 字段，默认值为 '玄幻' (或代码中定义的初始值)，以保证后端接口兼容性。
- **隐藏知识库关联**: 移除了"基本信息"中的"关联知识库"选择区域。
  - *注*: 数据层 `selectedKbIds` 默认为空数组。
- **隐藏侧边栏类型展示**: 隐藏了配置页侧边栏底部的"当前类型"信息展示框。

## 验证方式
- **UI 检查**:
  - 进入创建 -> 选择"小说" -> 进入配置页。
  - 确认"基本信息"标签页下仅显示：封面上传、作品名称、简介。
  - 确认侧边栏底部无"当前类型"展示。
- **功能验证**:
  - 填写标题和简介后提交，确认创建成功。
  - 确认控制台无报错。
