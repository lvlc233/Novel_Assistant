剩余的15个功能点:
- 作品类型相关
1. 在插件卡片中的作品类型列表展示操作 [已完成]
2. 在作品创建页面中的作品类型选择操作 [已完成]



- 插件工具化
3. 创建一个将插件转化为Agent工具的功能,可以利用我们的插件服务发现的功能并过滤其中的tags含有"tool"的插件,并约定tool类型的插件的工具来自操作定义,并通过__init__,也就是config来配置是否开启给Agent,
例如:

def __init__(self, 操作1_is_tool: bool = True, 操作2_is_tool: bool = True):
    self.操作1_is_tool = 操作1_is_tool
    self.操作2_is_tool = 操作2_is_tool

def 操作1()...

def 操作2()...

如果操作1_is_tool为True,则将操作1作为一个工具提供给Agent,否则不作为
然后前端中,使用AgentMananger管理所有Tool的开关,可以是一个插件一个卡片,然后一个卡片中提供所有的__init__中的操作定义,来控制工具的启动关闭

- 记忆插件
4. 实现记忆的curd的基础操作,和在卡片中的映射关系(记忆插件的数据结构就是list[{id:记忆id,name:记忆名称,context:记忆内容}])
5. 基于第3点的描述提供工具化的配置项

- documentAgent插件[完成]
6. 提供内嵌的能力(工具)(document阅读,document修改)
7. 提供sse对话能力(前后端连接)